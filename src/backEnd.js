export class UserAge {
  user(month, day, year) {
    this.month = month;
    this.day = day;
    this.year = year;
  }
  setMonth(month) {
    this.month = month;
  }
  setDay(day) {
    this.day = day;
  }
  setYear(year) {
    this.year = year;
  }

}
